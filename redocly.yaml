extends: [recommended]
rules:
  # Stylistic / optional — keep `docs:validate` focused on structural correctness
  # (the kind that breaks Swagger UI, codegen, and client SDK generation).
  operation-operationId: off
  no-unused-components: off
  operation-4xx-response: off
  no-server-example.com: off
  info-license: off
